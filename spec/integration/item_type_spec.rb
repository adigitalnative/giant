require 'spec_helper'

describe "A visitor" do
  it "cannot access item type management"
end
describe "A signed in user" do
  it "cannot access item type management"
end
describe "A signed in admin" do
  it "can create item types"
  it "can edit item types"
  it "can delete item types"
  it "can view a list of item types"
end